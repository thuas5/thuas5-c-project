#include <stdio.h>
#include <string.h>
int main()
{
	char ar1[1000],ar2[1000],len,len2;
	int i,j=0,k=0,x=0;
	gets(ar1);
	gets(ar2);
    len=strlen(ar1);
    len2=strlen(ar2);
    	if(len==len2)
    	{
    			printf("等于");
		}
		else{
				printf("不等于");
		}

//	printf("大于%d,等于%d,小于%d",j,k,x);
	
}
